<?php
declare(strict_types=1);

/**
 * Номер версии deeno — единственное место, где он задаётся.
 *
 * Файл подключают ОБА входа: корневой index.php (фронт-контроллер сайта) и
 * admin/index.php. Раньше константа объявлялась только в корневом index.php,
 * и на Apache, где /admin/ — реальная папка и запрос идёт в обход
 * фронт-контроллера, версия в админке была пустой («Система» на дашборде,
 * подпись в Настройках).
 */

if (!defined('DEENO_VERSION')) {
    define('DEENO_VERSION', '1.1.0');
}
